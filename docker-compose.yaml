version: "3.8"

services:
  mc-server:
    image: itzg/minecraft-server
    container_name: mc
    ports:
      - "${MC_PORT}:25565" 
    env_file:
      - .env
    volumes:
      - mc-data:/data
    restart: unless-stopped

volumes:
  mc-data: